import { View, Text, Dimensions, TouchableWithoutFeedback, Modal, StyleSheet } from 'react-native'
import React from 'react'

const BottomPopUp = () => {
  return (
   <Modal>
    
   </Modal>
  )
}

export default BottomPopUp